html
  body
    if error
     p= error.message


    form(action = '/courses', method = 'post')
      div
        label Название:
          input(type = 'text', name = 'title', value = title)
      div
        label Описание:
          input(name='description', required = true, value = description)
      input(type = 'submit', value = 'Создать')